import type { ExtractPublicPropTypes, PropType } from '@vue/runtime-core';

export interface MenuProps {
  // TODO: 定义属性
};

export const menuProps = {} as const;

export type menuInstance = ExtractPublicPropTypes<typeof menuProps>;
