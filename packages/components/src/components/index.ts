// 导出所有组件
// 随着组件的增加，需要在这里导出
import KButton from './k-button';
import KLabel from './k-label';
import KTable from './k-table';
import KConfigProvider from './k-config-provider';

export { KButton, KLabel, KTable, KConfigProvider };
