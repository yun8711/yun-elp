export type YunElpLanguage = {
  common: {
    confirm: string;
    cancel: string;
    notice: string;
    // clear: string;
    // close: string;
    // loading: string;
    // search: string;
    // more: string;
    // all: string;
  };
  empty: {
    description: string;
  };
  pop: {
    popContent: string;
  };
};
